---
# Direct NodePort for the long-lived-connection arm (scripts/S9_longconn.js,
# scripts/S10_longconn_with_metrics.sh). Bypasses the NGINX ingress so that
# keep-alive connections terminate on the quarkus-reactive-perf-jvm pod itself.
# Generated from nodeport-template.yaml.
apiVersion: v1
kind: Service
metadata:
  name: quarkus-reactive-perf-jvm-direct
  namespace: perf-test
  labels:
    app: quarkus-reactive-perf-jvm
    longconn: "true"
spec:
  type: NodePort
  selector:
    app: quarkus-reactive-perf-jvm
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
      nodePort: 31096
