---
apiVersion: v1
kind: Service
metadata:
  name: quarkus-perf-native
  namespace: perf-test
  labels:
    app: quarkus-perf-native
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: quarkus-perf-native
