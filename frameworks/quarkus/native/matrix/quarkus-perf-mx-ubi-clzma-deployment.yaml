# Generated by scripts/S8_gen_manifests.sh — do not edit by hand.
# Table 8a cell: base=ubi compression=clzma
apiVersion: apps/v1
kind: Deployment
metadata:
  name: quarkus-perf-mx-ubi-clzma
  namespace: perf-test
  labels:
    experiment: table8a
    base: ubi
    compression: clzma
spec:
  # Scaled by scripts/S8_upx_matrix.sh; idle at 0 so the matrix costs nothing
  # until it is being measured.
  replicas: 0
  selector:
    matchLabels:
      app: quarkus-perf-mx-ubi-clzma
  template:
    metadata:
      labels:
        # Unique per variant. S4_upx_pagemap_analysis.sh warns that the older
        # manifests reuse 'app' across native variants, which makes a label
        # selector silently match the wrong pods.
        app: quarkus-perf-mx-ubi-clzma
        experiment: table8a
        base: ubi
        compression: clzma
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/q/metrics"
    spec:
      containers:
        - name: quarkus-perf-mx-ubi-clzma
          image: ghcr.io/matejsaric32/quarkus-perf-mx-ubi-clzma:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: quarkus-perf-config
            - secretRef:
                name: perf-secret
          securityContext:
            # uid 1001 does not exist in quarkus-distroless-image:2.0, whose
            # unprivileged account is nonroot=65532. Set explicitly so the pod
            # spec states the run user rather than inheriting it from the image.
            runAsUser: 1001
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /q/health/live
              port: 8080
            initialDelaySeconds: 0
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /q/health/ready
              port: 8080
            initialDelaySeconds: 0
            periodSeconds: 10
            failureThreshold: 3
