---
apiVersion: v1
kind: Service
metadata:
  name: ktor-perf
  namespace: perf-test
  labels:
    app: ktor-perf
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: ktor-perf